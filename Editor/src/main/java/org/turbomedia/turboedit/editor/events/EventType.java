package org.turbomedia.turboedit.editor.events;

public enum EventType {
    THEME_CHANGED,
    CLIP_SELECTED,
    WINDOW_RESIZE,
    PREFERENCES_CHANGED,
    PLAYBACK_TOGGLE,
    PLAYBACK_SET_TIME
}
